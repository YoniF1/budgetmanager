<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=, initial-scale=1.0">
    <title>Document</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="/public/style.css" rel="stylesheet" type="text/css">
    <style>
        .container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding-bottom: 30px;
            height: 100vh; 
        }

        .form-container {
            width: 300px;
            margin-top: 50px
        }

        nav {
            position: fixed;
            width: 100%; 
            z-index: 1000;
        }

        .welcome-message {
            position: absolute;
            top: 100px;
            left: 100px;
            font-size: 24px;
            color: green;
            height: 10vh;
            width: 30vw;
        }

    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg bg-dark fixed-top">
        <div class="container-fluid">
          <p class="mt-3 text-secondary">Moses the Money Manager</p>
          <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
            <ul class="navbar-nav mb-2 mb-lg-0">
            <% if(isLoggedIn) { %>
                <li class="nav-item">
                    <a class="nav-link active text-secondary" aria-current="page" href="#">Home</a>
                    </li>
                    <li class="nav-item text">
                    <a class="nav-link active text-secondary" href="#">Expenses</a>
                    </li>
                <li class="nav-item">
                    <button>
                        <a class="nav-link active text-secondary" aria-current="page" href="/logout">Log out</a>
                    </button>
                </li>
            <%} %>
              </li>  
            </ul>
          </div>
        </div>
    </nav>

    <main class="container">
        <h1>Sign up</h1>
        <form action="/signup" method="POST">
            <label for="first_name">First name</label><br>
            <input type="text" id="first_name" name="first_name"><br>
            
            <label for="last_name">Last name</label><br>
            <input type="text" id="last_name" name="last_name"><br>
            
            <label for="email">Email</label><br>
            <input type="text" id="email" name="email"><br>
            
            <label for="password">Password</label><br>
            <input type="password" id="password" name="password"><br>
            <br>
            <button type="submit">Register</button>
        </form>
    </main>

        <% if (error) { %>
            <p style="color: red;"><%= error %></p>
        <% } %>

        <% if (user_exists) { %>
            <p style="color: red;"><%= user_exists %></p>
        <% } %>

        <% if (success) { %>
            <p style="color: green;"><%= success %></p>
        <% } %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>